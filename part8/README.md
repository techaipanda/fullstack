# part8 — GraphQL(新 MOOC.fi 课程)

> **课程 URL 更新**:课程 Full Stack Open 第 8 部分已迁移到新地址:
> https://courses.mooc.fi/org/uh-cs/courses/full-stack-open-graphql
>
> 老的 https://fullstackopen.com/en/part8 链接仍可访问但不再更新,**新内容以 MOOC.fi 为准**。

## 课程章节(新 MOOC.fi 划分)

| 课程章节 | 标题 | 对应子项目 | 状态 |
|---|---|---|---|
| Chapter 1 | Getting Started | (跳过 — 纯理论) | — |
| Chapter 2 | Apollo Server | part8 a — Apollo Server(v4 standalone)| ✅ 已完结 |
| Chapter 2 | Apollo Studio Explorer | part8 b — Apollo Studio Explorer(观察性小节,无新增代码)| ✅ 已完结 |
| Chapter 2 | Schema syntax highlighting in VS Code | part8 c — Schema syntax highlighting(`/* GraphQL */` + VS Code 扩展)| ✅ 已完结 |
| Chapter 2 | Parameters of a resolver | part8 d — Parameters of a resolver(解释性小节,讲透 `(root, args, context, info)` 4 参数签名)| ✅ 已完结 |
| Chapter 2 | The default resolver | part8 e — The default resolver(兑现 part8d 伏笔:`Person: { name: (root) => root.name, ... }` 5 字段显式 default resolver + partial override)| ✅ 已完结 |
| Chapter 2 | Object within an object | part8 f — Object within an object(嵌套 GraphQL type + `Person.address: Address!` + `Person.address` 自定义 resolver 重新组装 Address)| ✅ 已完结 |
| Chapter 2 | Mutations | part8 g — Mutations(`type Mutation { addPerson }` + `Mutation.addPerson` resolver + `uuid()` 生成 id + `persons.concat(person)` 重赋值)| ✅ 已完结 |
| Chapter 2 | Error handling | part8 h — Error handling(`Mutation.addPerson` 加查重 + 抛 `GraphQLError` 带 `extensions.code = 'BAD_USER_INPUT'` + `invalidArgs`)| ✅ 已完结 |
| Chapter 2 | Enum | part8 i — Enum(`enum YesNo { YES NO }` + `Query.allPersons(phone: YesNo)` 加 nullable enum 过滤参数 + resolver 按 `'YES'` / `'NO'` 字符串过滤)| ✅ 已完结 |
| Chapter 2 | Changing a phone number | part8 j — Changing a phone number(`Mutation.editNumber(name, phone): Person` + 不可变 `persons.map` 更新 + 找不到 person 返回 null)| ✅ 已完结 |
| Chapter 2 | More on queries | (待映射 part8 字母) | ⏳ 待规划(combined queries + aliases — Chapter 2 收尾小节)|
| Chapter 3 | Apollo client | **part8 k** — Apollo client(`@apollo/client` + `ApolloClient` + `HttpLink` + `InMemoryCache` + `gql\`\`` + `client.query().then(console.log)` + `ApolloProvider` 包 `<App />` + 走 `@apollo/client/react` 子路径)| ✅ 已完结 |
| Chapter 3 | Making queries | **part8 l** — Making queries(`useQuery(ALL_PERSONS)` Hook 替代 part8k 的 `client.query().then(console.log)` + `result.loading` 判 loading 态 + 抽 `src/components/Persons.jsx` 子组件 + Container/Presentational 雏形)| ✅ 已完结 |
| Chapter 3 | Making queries | (待映射 part8 l) | ⏳ 待规划 |
| Chapter 3 | Named queries and variables | **part8 m** — Named queries and variables(GraphQL query 加 `query findPersonByName(...)` operation name + `$nameToSearch: String!` variables + 在 Persons 子组件里加 `useState(null)` + `useQuery(FIND_PERSON, { variables, skip: !nameToSearch })` 实现"按需发请求" + 抽 `Person` 子组件展示详情 + H3 "Cache" 子段)| ✅ 已完结 |
| Chapter 3 | Doing mutations | **part8 n** — Doing mutations(`CREATE_PERSON` mutation 用 `mutation createPerson($name: String!, $street: String!, $city: String!, $phone: String)` variables + 新建 `src/components/PersonForm.jsx` 用 4 个独立 `useState('')` + `useMutation(CREATE_PERSON)` 数组解构 + submit handler `event.preventDefault()` + `createPerson({ variables })` 乐观清空表单 + 改 `src/App.jsx` 加 `<PersonForm />` 渲染。**故意不解决 "mutation 后 UI 不刷新"** — 留 part8o "Updating the cache" 处理)| ✅ 已完结 |
| Chapter 3 | Updating the cache | **part8 o** — Updating the cache(`CREATE_PERSON` mutation 加 `refetchQueries: [{ query: ALL_PERSONS }]` 让 Persons 列表立即看到新 person + 抽 `src/queries.js` 统一导出 3 个 GraphQL 操作(`ALL_PERSONS` / `FIND_PERSON` / `CREATE_PERSON`)+ `App.jsx` 改 `import { ALL_PERSONS } from './queries'` + `Persons.jsx` 改 `import { FIND_PERSON } from '../queries'` + `PersonForm.jsx` 改 `import { ALL_PERSONS, CREATE_PERSON } from '../queries'`,移除各文件无用 `gql` import。**故意不解决 "他人修改自动同步"** — refetchQueries 只在当前用户 mutation 后触发,留 part8p+ 用 Subscription 解决)| ✅ 已完结 |
| Chapter 3 | Handling mutation errors | **part8 p** — Handling mutation errors(`useMutation` 加 `onError: (error) => setError(error.message)` option 捕获 server 抛的 GraphQLError + 新建 `src/components/Notify.jsx` 纯展示组件(`errorMessage` prop + `return null` 无错时不渲染 + `style={{ color: 'red' }}` 红字) + 改 `src/App.jsx` 加 `errorMessage` state + `notify` 函数(`setErrorMessage + setTimeout(10000)` 自动清除) + JSX 加 `<Notify errorMessage={errorMessage} />` + `<PersonForm setError={notify} />` 完成 setError prop 链)| ✅ 已完结 |
| Chapter 3 | Updating a phone number | **part8 q** — Updating a phone number(`queries.js` 加 EDIT_NUMBER mutation + 新建 `src/components/PhoneForm.jsx` 用 `useMutation(EDIT_NUMBER)` + `App.jsx` 渲染 `<PhoneForm setError={notify} />` + 用 **`onCompleted: (data) => { if (!data.editNumber) setError('person not found') }`** 替代 `onError` —— 因为 server `editNumber` 找不到 person 时返回 null 不抛 GraphQLError,所以 `onError` 没用,必须用 `onCompleted`。**Apollo cache 自动更新**:mutation 返回的 Person 按 ID 归一化,Persons 列表立即看到新 phone,不需要 `refetchQueries`(per course line 502))| ✅ 已完结 |
| Chapter 3 | Apollo Client and the applications state | (待映射 part8 字母) | ⏳ 待规划(标题是课程原文 typo `applications state`,verbatim 沿用)|
| Chapter 3 | Exercises 8. Authors view / 9. Books view / 10. Adding a book / 11. Authors birth year / 12. Authors birth year advanced | (跳过 — 与 part7 练习策略一致,不做练习题) | ⏭️ 跳过 |
| Chapter 4 | Database and user administration | **part8 r** — Refactoring the backend(单文件 Apollo Server 拆成 schema.js + resolvers.js + server.js + index.js stub 4 文件 + dotenv ^16.4.5 + factory function `startServer(port)` + persons 数组从 schema.js 搬到 resolvers.js + `process.env.PORT \|\| 4000` 环境变量读 port)| ✅ 已完结 |
| Chapter 4 | Mongoose and Apollo | **part8 s** — Mongoose and Apollo(在 part8r 4 文件基础上加 db.js + models/person.js 共 6 文件模块 + mongoose ^8.9.0 + dotenv 读 MONGODB_URI + connectToDatabase(uri) 工厂 + try/catch + `process.exit(1)` + index.js 加 `main = async () => { await connectToDatabase(MONGODB_URI); startServer(PORT) }` + resolvers.js 大改:in-memory `persons` 数组删 + 删 `uuid` import + `Person = require('./models/person')` mongoose model + 全部 resolver 转 `async` + 用 `Person.collection.countDocuments()` / `Person.find` / `Person.findOne` / `await Person.exists` / `person.save()` 替代数组操作 + **`// filters missing` 占位 comment 保留** verbatim 课程 line 83(phone filter 是后续小节的内容,不是 part8t))| ✅ 已完结 |
| Chapter 4 | Validation | **part8 t** — Validation(在 part8s 6 文件基础上**仅改 resolvers.js 一个文件** — verbatim 课程 Validation 段 line 16-28 addPerson + line 39-51 editNumber 两个 `await person.save()` 加 try/catch + catch 抛 `GraphQLError(`Saving person failed: ${error.message}` / `Saving number failed: ${error.message}`, { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name, error }})` — `extensions.error` 字段塞 mongoose 原始 error 对象 + Query 块 / Person 块 / schema.js / models/person.js / db.js / server.js / index.js 全部沿用 part8s verbatim + **`// filters missing` 占位仍保留** — 课程 Validation 段**不**包含 phone filter,phone filter 是 part8s README 提到的 line 149-162 但实际在更后续小节才补 + 课程 branch 标注 "part8-4" 暗示 Validation 节属于 Chapter 4 第 3 子节 — **GraphQL ↔ Mongoose 双重防线**:GraphQL SDL(per schema.js)类型校验 + Mongoose schema(per models/person.js)`minlength: 5/3` + `required: true` 字段验证 + catch 只包 save() 精准防御)+ 前端 part8p `onError: (error) => setError(error.message)` 接住 extensions.code 'BAD_USER_INPUT' 显示红字 Notify + 课程原文"the frontend can then display this information to the user, who can try the operation again with a better input"实现前后端错误链路闭环)| ✅ 已完结 |
| Chapter 4 | User and log in | **part8 u** — User and log in(在 part8t 6 文件基础上**新增 1 文件 + 改 5 文件** — 新建 `models/user.js` verbatim 课程 block 59(username required+minlength:3 + friends 数组 ObjectId ref Person)+ 改 `package.json` 加 `jsonwebtoken: ^9.0.2` 依赖(per course block 65-66)+ 改 `schema.js` 加 `type User` + `type Token` + `Query.me: User` + `Mutation.createUser(username: String!): User` + `Mutation.login(username: String!, password: String!): Token`(per course block 63 verbatim)+ 改 `resolvers.js` 顶部加 `const jwt` + `const User` + Query.me 用 `(root, args, context) => context.currentUser` 三参数签名(per course block 84)+ Mutation.createUser 用 `.save().catch(error => throw GraphQLError(`Creating the user failed: ...`, { extensions: { code: 'BAD_USER_INPUT', invalidArgs, error }}))` 错误处理风格(per course block 68)+ Mutation.login 用 `if (!user || args.password !== 'secret') throw GraphQLError('wrong credentials')` + `jwt.sign({ username, id }, process.env.JWT_SECRET)` 返回 `{ value: token }`(per course block 68)+ 改 `server.js` 顶部加 `const jwt` + `const User` + **新增** `getUserFromAuthHeader(auth)` helper(verbatim 课程 block 79 highlighted lines 9-16:auth 不以 'Bearer ' 开头 return null + `jwt.verify(auth.substring(7), JWT_SECRET)` + `User.findById(id).populate('friends')`)+ **新增** context 字段在 `startStandaloneServer` 第二参数里(verbatim 课程 block 79 highlighted lines 26-30: `context: async ({ req }) => { const auth = req.headers.authorization; const currentUser = await getUserFromAuthHeader(auth); return { currentUser } }`)+ 改 `.env.example` 加 `JWT_SECRET=replace-this-with-a-strong-random-secret` 占位(per course block 69 注明 "JWT_SECRET must be defined in the .env file")+ db.js / index.js / models/person.js 全部沿用 part8t verbatim + **课程故意简化**:password = 'secret' 硬编码(per course block 57 "all users have the same password which is hardcoded to the system"),**不**用 bcrypt、**不**存 passwordHash、**不**实现 addFriend(只预留 friends schema 字段))| ✅ 已完结 |
| Chapter 4 | Friends list | **part8 v** — Friends list(在 part8u 6 文件基础上**改 2 文件**,不动其他)— 改 `schema.js` 在 `Mutation` 块加 `addAsFriend(name: String!): User`(per course block 94 highlighted line 3)+ 改 `resolvers.js` 的 `Mutation.addPerson` 签名 `(root, args)` → `(root, args, context)` + 加 `currentUser = context.currentUser` + 加 `if (!currentUser) throw GraphQLError('not authenticated', { extensions: { code: 'UNAUTHENTICATED' }})`(per course block 91 highlighted lines 2-11)+ try 块加 `currentUser.friends = currentUser.friends.concat(person)` + `await currentUser.save()`(per course block 91 highlighted lines 28-29)+ **新增** `Mutation.addAsFriend: async (root, args, { currentUser }) => {...}` 完整 resolver(per course block 96:UNAUTHENTICATED 鉴权 + `nonFriendAlready = (person) => !currentUser.friends.map((f) => f._id.toString()).includes(person._id.toString())` helper + `Person.findOne({ name: args.name })` + 不存在 throw `GraphQLError("The name didn't found", { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name }})`(per course 原文英文别扭但严格 verbatim)+ `if (nonFriendAlready(person)) currentUser.friends = currentUser.friends.concat(person)` + `await currentUser.save()` + return currentUser)+ `server.js` / `models/{user,person}.js` / `index.js` / `db.js` / `package.json` / `.env.example` 全部沿用 part8u verbatim + **课程故意**:editNumber **不**加鉴权(per course block 88 文字"adding and editing persons requires logging in"但 block 91-103 只展示 addPerson 不展示 editNumber,留后续章节)+ allPersons **不**按 `currentUser.friends` 过滤(block 88 文字提"personalized view"但 block 91-103 不展示,留后续)+ `// filters missing` 占位仍保留(phone filter 仍未补)+ **新课程错误码** `UNAUTHENTICATED`(per course block 91/96)| ✅ 已完结 |
| Chapter 5 | Login and updating the cache | **part8 w** — User login(verbatim 课程 User login 子节 + 3 个 stub 占位组件)| ✅ 已完结 |
| Chapter 5 | Adding a token to a header | (待映射 part8 字母) | ⏳ 待规划 |
| Chapter 5 | Fixing validations | (待映射 part8 字母) | ⏳ 待规划 |
| Chapter 5 | Updating cache, revisited | (待映射 part8 字母) | ⏳ 待规划 |
| Chapter 5 | Exercises 18-24 | (跳过 — library app 练习,与 part7 练习策略一致) | ⏭️ 跳过 |
| Chapter 6 | Fragments and subscriptions | (待映射 part8 字母) | ⏳ 待规划 |

## 子项目列表

- [`a-apollo-server/`](./a-apollo-server/) — Apollo Server v4 standalone(verbatim Chapter 2 "Apollo Server" 段)
- [`b-apollo-studio-explorer/`](./b-apollo-studio-explorer/) — Apollo Studio Explorer walkthrough(verbatim Chapter 2 "Apollo Studio Explorer" 段;观察性小节,代码复用 part8a)
- [`c-schema-syntax-highlighting/`](./c-schema-syntax-highlighting/) — VS Code GraphQL 语法高亮(`/* GraphQL */` + `GraphQL.vscode-graphql` 扩展;代码复用 part8a)
- [`d-parameters-of-resolver/`](./d-parameters-of-resolver/) — Resolver 4 参数签名讲解(`(root, args, context, info)` + `args` 形状 + `root` 顶层未用;纯解释性小节,代码复用 part8a)
- [`e-the-default-resolver/`](./e-the-default-resolver/) — Apollo default resolver 机制(`Person: { name: (root) => root.name, phone: (root) => root.phone, ... }` 5 字段全显式 + partial override 示例 — 把 street/city 硬编码为 Manhattan / New York)
- [`f-object-within-an-object/`](./f-object-within-an-object/) — 嵌套 GraphQL type(`type Address { street, city }` + `Person.address: Address!` + 自定义 `Person.address: (root) => ({ street: root.street, city: root.city })` resolver)
- [`g-mutations/`](./g-mutations/) — GraphQL Mutation(`type Mutation { addPerson(name, phone, street, city): Person }` + `Mutation.addPerson: (root, args) => { persons = persons.concat({...args, id: uuid()}); return person }` + `const persons` 改 `let` + `uuid` v1 npm 包)
- [`h-error-handling/`](./h-error-handling/) — GraphQL Error handling(`Mutation.addPerson` 加查重 + 抛 `GraphQLError('Name must be unique: ...', { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name } })` — 兑现 part8g Step 9 末尾伏笔,GraphQL 校验的两层防线:Schema 层(自动)vs Resolver 层(手动,业务规则))
- [`i-enum/`](./i-enum/) — GraphQL Enum(`enum YesNo { YES NO }` + `Query.allPersons(phone: YesNo)` nullable enum 过滤参数 + `allPersons` resolver 按 `args.phone === 'YES' ? person.phone : !person.phone` 字符串比较过滤 — 兑现 part8h 伏笔:不是所有业务规则都该抛错,有时候应该软过滤)
- [`j-changing-a-phone-number/`](./j-changing-a-phone-number/) — Mutation Update(`Mutation.editNumber(name, phone): Person` + 不可变 `persons = persons.map(p => p.name === args.name ? { ...person, phone: args.phone } : p)` 更新 + 找不到 person `return null` — 兑现 part8i 伏笔:update semantics + 不可变模式 + 找不到返回 null)
- [`k-apollo-client/`](./k-apollo-client/) — **Chapter 3 第 1 子节**(架构大切换:服务端 → 客户端)— Vite + React + Apollo Client:`npm install @apollo/client graphql` + `new ApolloClient({ link: new HttpLink({ uri: 'http://localhost:4000' }), cache: new InMemoryCache() })` + `gql\`query { allPersons { name phone address { street city } id } }\`` + `client.query({ query }).then(response => console.log(response.data))` + `ApolloProvider` 走 `@apollo/client/react` 子路径包裹 `<App />`。**需要 part8j server 跑着才能验证** — 两个终端,4000(GraphQL server)+ 5173(Vite dev)
- [`l-making-queries/`](./l-making-queries/) — **Chapter 3 第 2 子节**(从命令式 → 声明式)— Vite + React + Apollo Client `useQuery`:删除 main.jsx 的 `client.query(...).then(console.log)` 段 + App.jsx 改 `useQuery(ALL_PERSONS)` + `result.loading` 判 `<div>loading...</div>` + 抽 `src/components/Persons.jsx` Presentational 子组件 + Container/Presentational 雏形。**ApolloProvider 在本章变成强依赖**(注释掉就报"Could not find Apollo Client context")。**需要 part8j server 跑着才能验证** — 同 part8k 双终端
- [`m-named-queries-and-variables/`](./m-named-queries-and-variables/) — **Chapter 3 第 3 子节**(query 命名 + 变量 + 按需发请求)— Vite + React + Apollo Client:**只改 `src/components/Persons.jsx` 一个文件**(课程明示)— GraphQL query 加 `query findPersonByName($nameToSearch: String!)` operation name + variables + Persons 内部 `useState(null)` 管 nameToSearch + `useQuery(FIND_PERSON, { variables: { nameToSearch }, skip: !nameToSearch })` 实现"按需发请求"+ 抽 `Person` 子组件展示单条详情 + 列表每行加 `<button onClick={() => setNameToSearch(p.name)}>show address</button>` 触发切换。**App.jsx / main.jsx / package.json / vite.config.js / index.html / .gitignore 全部沿用 part8l verbatim**(课程本节仅改一个文件)。**H3 "Cache" 子段**:Apollo `InMemoryCache` 按 `query name + variables` 做 cache key,同 key 第二次不发网络请求。**需要 part8j server 跑着才能验证** — 同 part8k/l 双终端
- [`n-doing-mutations/`](./n-doing-mutations/) — **Chapter 3 第 4 子节**(读 → 读+写)— Vite + React + Apollo Client **useMutation**:新建 `src/components/PersonForm.jsx`(CREATE_PERSON mutation + 4 个独立 useState('') + `useMutation(CREATE_PERSON)` 数组解构 + submit handler `event.preventDefault()` + `createPerson({ variables })` + 乐观清空表单) + 改 `src/App.jsx`(加 `import PersonForm` + 渲染 `<PersonForm />`)。**Persons.jsx / main.jsx / package.json 沿用 part8m verbatim**。**故意不做"mutation 后 UI 自动刷新"** — Apollo InMemoryCache 自动归一化但不自动触发 re-render,这是 part8o "Updating the cache" 才用 `pollInterval` / `refetchQueries` 解决的问题。**需要 part8j server 跑着才能验证** — 同 part8k/l/m 双终端
- [`o-updating-the-cache/`](./o-updating-the-cache/) — **Chapter 3 第 5 子节**(mutation 后 UI 自动刷新)— Vite + React + Apollo Client **refetchQueries**:**新建 `src/queries.js`**(verbatim 课程:导出 `ALL_PERSONS` / `FIND_PERSON` / `CREATE_PERSON` 三个 GraphQL 操作)+ 改 `src/App.jsx`(抽 inline ALL_PERSONS 定义到 `import { ALL_PERSONS } from './queries'`,移除无用 `gql` import)+ 改 `src/components/Persons.jsx`(抽 inline FIND_PERSON 定义到 `import { FIND_PERSON } from '../queries'`,移除无用 `gql` import)+ 改 `src/components/PersonForm.jsx`(抽 inline CREATE_PERSON 定义到 `import { ALL_PERSONS, CREATE_PERSON } from '../queries'`,移除无用 `gql` import + **`useMutation(CREATE_PERSON, { refetchQueries: [{ query: ALL_PERSONS }] })`** —— mutation 成功后 Apollo 自动重发 ALL_PERSONS,Persons 列表立即看到新 person)。**main.jsx / package.json / vite.config.js / index.html / .gitignore 全部沿用 part8n verbatim**。**Apollo cache 三种更新策略对比**:pollInterval(定时轮询,缺点:多余流量+闪烁)/ refetchQueries(mutation 后精准重发,缺点:仅当前用户同步)/ cache.modify(编程式,part8p+ 才讲)。**故意不做"他人修改自动同步"** —— refetchQueries 只在当前用户 mutation 后触发,留 part8p+ 用 GraphQL Subscription 解决。**需要 part8j server 跑着才能验证** — 同 part8n 双终端
- [`p-handling-mutation-errors/`](./p-handling-mutation-errors/) — **Chapter 3 第 6 子节**(mutation 失败时 UI 反馈)— Vite + React + Apollo Client **onError**:**新建 `src/components/Notify.jsx`**(verbatim 课程:Presentational 组件,接收 `errorMessage` prop,`!errorMessage` 时 `return null` 不渲染,有值时返回 `<div style={{ color: 'red' }}>{errorMessage}</div>` 红字 div)+ 改 `src/App.jsx`(加 `import { useState } from 'react'` + `import Notify from './components/Notify'` + `const [errorMessage, setErrorMessage] = useState(null)` + `const notify = (message) => { setErrorMessage(message); setTimeout(() => { setErrorMessage(null) }, 10000) }` + JSX 加 `<Notify errorMessage={errorMessage} />` 在最上面 + `<PersonForm setError={notify} />` 透传 notify)+ 改 `src/components/PersonForm.jsx`(函数签名改 `const PersonForm = ({ setError }) => {` 接收 prop + `useMutation(CREATE_PERSON, { refetchQueries: [...], onError: (error) => setError(error.message) })` —— `onError` 与 `refetchQueries` 互斥:成功走 refetch,失败走 onError)。**src/queries.js / src/components/Persons.jsx / src/main.jsx / package.json / vite.config.js / index.html / .gitignore 全部沿用 part8o verbatim**。**setError prop 链设计**:App 管 errorMessage state + 10s 自动清除 + Persons 不需要这个 prop 因为只读 + PersonForm 通过 setError prop 把"mutation 失败"传回 App → notify → Notify 红字。**故意不做"form 在失败时不清空"** —— 课程 submit handler 仍按 part8n 立即清空,生产代码应该在 `onCompleted` 里清。**需要 part8j server(part8h 已经抛 'Name must be unique' GraphQLError)跑着才能验证** — 同 part8o 双终端
- [`q-updating-a-phone-number/`](./q-updating-a-phone-number/) — **Chapter 3 第 7 子节**(update mutation + success-but-null 错误处理)— Vite + React + Apollo Client **onCompleted**:改 `src/queries.js`(在 part8p 基础上加第 4 个 GraphQL 操作 `EDIT_NUMBER` — `mutation editNumber($name: String!, $phone: String!) { editNumber(name: $name, phone: $phone) { name phone address { street city } id } }`)+ **新建 `src/components/PhoneForm.jsx`**(verbatim 课程 final-state line 512:函数签名 `const PhoneForm = ({ setError }) => {` + 2 个独立 useState(name + phone)+ `useMutation(EDIT_NUMBER, { onCompleted: (data) => { if (!data.editNumber) { setError('person not found') } } })` —— **关键**:用 `onCompleted` 不用 `onError`,因为 server `editNumber` 找不到 person 时返回 null(per part8j resolver)不抛 GraphQLError,所以 mutation 整体是"成功响应"+ submit handler `event.preventDefault()` + `changeNumber({ variables: { name, phone } })` + `setName('')` + `setPhone('')` + JSX form with 2 个 input + 1 个 button "change number")+ 改 `src/App.jsx`(加 `import PhoneForm from './components/PhoneForm'` + JSX 在 `<PersonForm />` 后面加 `<PhoneForm setError={notify} />` —— **setError prop 链复用**,notify 同一个函数服务于 PersonForm 和 PhoneForm)。**src/components/Notify.jsx / src/components/PersonForm.jsx / src/components/Persons.jsx / src/main.jsx / package.json / vite.config.js / index.html / .gitignore 全部沿用 part8p verbatim**。**onError vs onCompleted 决策树**:server 抛 GraphQLError → onError(error.message)(PersonForm 模式)vs server 成功但 data 字段为 null → onCompleted(data)(PhoneForm 模式)。**Apollo cache 自动更新**:EDIT_NUMBER 返回的 Person 按 ID 归一化,Persons 列表立即看到新 phone,不需要 refetchQueries(per course line 502)。**需要 part8j server 跑着才能验证** — 同 part8p 双终端
- [`r-refactoring-the-backend/`](./r-refactoring-the-backend/) — **Chapter 4 第 1 子节**(后端架构大切换:单文件 Apollo Server → 4 文件模块化)— **纯后端**(跟 part8a~j 同类,无前端代码):拆 `index.js` 单文件 → `schema.js`(只导出 typeDefs SDL 字符串,单一职责)+ `resolvers.js`(只导出 resolvers + 内含 persons 数组 3 人:Arto + Matti + Venla,per course line 67-88 verbatim)+ `server.js`(只导出 `startServer(port)` 工厂函数,Apollo Server v4 standalone 启动器封装)+ `index.js` 重写为 **5 行 stub**:`require('dotenv').config()` + `require('./server')` + `const PORT = process.env.PORT || 4000` + `startServer(PORT)`。新增依赖 **`dotenv ^16.4.5`**(per course line 171 `npm install dotenv`)。**关键架构决策**:`module.exports = typeDefs` / `module.exports = resolvers` / `module.exports = startServer` 三者都用**单值默认导出**(不是聚合对象),require 拿到的是 string/object/function 本身。**factory function 模式**:`require('./server')` 不立即启动,index.js 显式调 `startServer(PORT)` 才启动 — 为后续接 MongoDB(`await mongoose.connect(...)`)留扩展空间(per course line 188-189)。**dotenv 兜底链**:`.env` 文件有 PORT → 用 env;没 .env 或没 PORT → fallback 4000(前端默认期望端口)。**persons 数组从 schema.js 搬到 resolvers.js**(per course line 142):数据 + 逻辑在同一个模块,但这是临时简化,后续 Mongoose 子节会替成 MongoDB。**故意不做**:不创建 `.env` 文件(课程没强制内容)/ 不装 `node_modules`(`npm install` 留给用户)/ 不拆更细的模块(resolvers 不再拆 query/mutation/person)。**这是 part8 第二个"架构大切换"**(第一个是 part8k 服务端→客户端,这次是单文件→多文件模块化)
- [`s-mongoose-and-apollo/`](./s-mongoose-and-apollo/) — **Chapter 4 第 2 子节**(从 in-memory 数组 → MongoDB 持久化)— **纯后端**(part8r 续):在 part8r 4 文件基础上**加 `db.js`**(verbatim 课程 line 43-59 `connectToDatabase(uri)` 工厂 + `try/catch` 包裹 `await mongoose.connect(uri)` + catch 里 `process.exit(1)` 终止进程,只导出 connectToDatabase 工厂)+ **加 `models/person.js`**(verbatim 课程 line 11-37 mongoose.Schema 5 字段:name/phone/street/city + 4 个 `minlength` 验证 + 3 个 `required: true` 验证,**注意 phone 没有 required 允许缺失**对应 part8r Venla 数据 + `module.exports = mongoose.model('Person', schema)` 只导出 model 实例)+ **改 `index.js`**(从 5 行扩到 9 实际代码:加 `require('./db')` + 加 `const MONGODB_URI = process.env.MONGODB_URI` 课程 verbatim **无 fallback** + 加 `const main = async () => { await connectToDatabase(MONGODB_URI); startServer(PORT) }` + 末尾 `main()` 调用,课程 verbatim **不接 .catch()**)+ **改 `resolvers.js`**(per course line 75-126 大改:删 in-memory `let persons = [...]` 数组 + 删 `uuid` import + 加 `const Person = require('./models/person')` 拿 mongoose model + **Query/Mutation 全部转 `async`** + `personCount` 改 `Person.collection.countDocuments()` + `findPerson` 改 `Person.findOne({ name: args.name })` + `addPerson` 改 `await Person.exists` 查重 + `new Person({ ...args })` + `person.save()` + `editNumber` 改 `Person.findOne` + `person.phone = args.phone` 直接修改字段 + `person.save()` 持久化,Person 块的 `address` resolver **不**加 async 保持纯 JS 对象构造 + **allPersons 保留 verbatim `// filters missing` 占位 comment 不提前补全** — 那是后续小节的内容,不是 part8t)+ **新增 `.env.example`**(只有占位无真实密码,`MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster.../phonebook` + `PORT=4000`,**不**创建真实 `.env` 避免泄露 Atlas 密码,被 `.gitignore` 排除)+ **新增 `mongoose ^8.9.0`** 依赖。**核心架构决策**(per course line 69):因为 CommonJS 顶层不支持 `await`,必须把 `await connectToDatabase(...)` 包在 `async () => { ... }` 函数里,然后 `main()` 调用 — 这是 Node.js CommonJS 项目的标准 main async 启动模式。**Apollo Promise resolve 行为**(per course line 130):resolver 返回 Promise 时 Apollo 自动 await 拿到 resolve 后的值,所以 `Person.find({})` / `person.save()` 这种返回 Promise 的 mongoose 操作直接 return 即可。**故意不做**:不给 `MONGODB_URI` 加 fallback / 不给 main() 加 `.catch()` / 不提前补全 allPersons phone filter / 不加 mongoose hooks(`pre('save')`)和 timestamps / 不创建 seed 数据(用户手动 addPerson)/ 不给 Person 块的 address resolver 加 async / 不把 `process.exit(1)` 改 `throw`。**运行前置**:需要 Atlas 集群或本地 `mongod` 跑着 + 用户自己 `cp .env.example .env` 改 URI。**下一步**是 part8t Validation 错误处理
- [`t-validation/`](./t-validation/) — **Chapter 4 第 3 子节**(mongoose schema 验证失败 → GraphQLError 透传前端)— **纯后端**(part8s 续,**最小改动** — 仅改 resolvers.js 一个文件):改 `resolvers.js` 的 addPerson + editNumber 两个 `await person.save()` 加 try/catch(verbatim 课程 Validation 段 line 16-28 + line 39-51)+ catch 抛 `new GraphQLError(`Saving person failed: ${error.message}` / `Saving number failed: ${error.message}`, { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name, error }})` — **extensions 三字段设计**:`code` 标准化错误码 / `invalidArgs` 告诉前端哪个参数错 / `error` 塞 mongoose 原始 ValidationError 对象给前端调试用(per course line 49-54 verbatim)。**GraphQL ↔ Mongoose 双重防线**:GraphQL SDL(per schema.js)声明 `String!` 自动验证类型 + Mongoose schema(per models/person.js) `minlength: 5/3` + `required: true` 验证字段值长度和存在 — 即使有人绕过 GraphQL 直连 server(比如 server 端 `new Person({})`),mongoose 兜底验证(per course line 41-44 暗示)。**try/catch 精准防御**:只包 `save()` 不包 `findOne`/`exists`/`find` — 因为只有 save() 触发 mongoose schema 验证,查询操作不校验字段。**addPerson vs editNumber catch message 不同**:前者 `Saving person failed: ...` / 后者 `Saving number failed: ...`(per course line 19 vs line 42)课程明示两段不同,我严格 verbatim。**前端 part8p 闭环**:part8t 后端抛 GraphQLError → Apollo 序列化 errors[] → 前端 part8p `useMutation(CREATE_PERSON, { onError: (error) => setError(error.message) })` 接住 → setError prop 链 → Notify 红字 div 展示(课程原文"the frontend can then display this information to the user, who can try the operation again with a better input")。**Query 块 / Person 块 / schema.js / models/person.js / db.js / server.js / index.js / package.json 全部沿用 part8s verbatim**(课程 Validation 段明示 only resolvers.js 改 try/catch)。**`// filters missing` 占位仍保留** — 课程 Validation 段**不**包含 phone filter,phone filter 仍是后续小节的内容(可能是 part8u 之后)。**故意不做**:不改 schema.js / 不改 models/person.js 验证规则 / 不接前端 onError 重构 / 不抓 ValidationError 子类型(课程只用通用 catch)/ 不把 error.message 转 i18n(课程原文用英文)。**运行前置**:同 part8s(Atlas 集群或本地 `mongod` 跑着 + `.env` 配 URI)。**下一步**是 part8u "User and log in"(per course 当前章节 line 16111 — User schema + bcrypt 密码哈希 + jsonwebtoken token 签发 + Login mutation + 当前 user context)
- [`u-user-and-log-in/`](./u-user-and-log-in/) — **Chapter 4 第 4 子节**(从无用户 → User 系统 + JWT token + Apollo context)— **纯后端**(part8t 续):**新增** `models/user.js`(verbatim 课程 block 59:`username: { type, required, minlength: 3 }` + `friends: [{ type: ObjectId, ref: 'Person' }]` + `mongoose.model('User', schema)`)— **课程故意简化**:所有用户共用 password = 'secret' 硬编码(per course block 57 "all users have the same password which is hardcoded to the system ... because our focus is on GraphQL, we will leave out all that extra hassle this time"),**不**用 bcrypt、**不**存 passwordHash、**不**实现 addFriend(只预留 friends schema 字段,实际增删留给 Friends list 子节)。**改** `package.json` 加 `jsonwebtoken: ^9.0.2` 依赖(per course block 65-66 `npm install jsonwebtoken`)。**改** `schema.js` 加 `type User { username, friends: [Person!]!, id }` + `type Token { value: String! }` + `Query.me: User` + `Mutation.createUser(username: String!): User` + `Mutation.login(username, password): Token`(per course block 63 verbatim)。**改** `resolvers.js` 顶部加 `const jwt = require('jsonwebtoken')` + `const User = require('./models/user')`(per course block 68)+ Query.me 用 `(root, args, context) => context.currentUser` 三参数签名(per course block 84 verbatim)— 第三个参数 context 由 Apollo 自动注入。**Mutation.createUser** 用 `.save().catch(error => throw GraphQLError(`Creating the user failed: ${error.message}`, { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.username, error }}))` 错误处理风格(per course block 68)— 跟 part8t addPerson 的 try/catch await 风格**不同**,课程**两种都展示**我都 verbatim 不统一。**Mutation.login** 用 `await User.findOne({ username })` + `if (!user || args.password !== 'secret') throw GraphQLError('wrong credentials', { extensions: { code: 'BAD_USER_INPUT' }})` + `jwt.sign({ username: user.username, id: user._id }, process.env.JWT_SECRET)` 返回 `{ value: token }`(per course block 68)— 课程原文(per course block 69):"Note that the JWT_SECRET must be defined in the .env file"。**改** `server.js` 顶部加 `const jwt` + `const User` + **新增** `getUserFromAuthHeader(auth)` helper(verbatim 课程 block 79 highlighted lines 9-16:三分支 — `auth` undefined 或不以 `Bearer ` 开头 return null + 否则 `jwt.verify(auth.substring(7), JWT_SECRET)` + `User.findById(decodedToken.id).populate('friends')`)+ **新增** context 字段在 `startStandaloneServer` 第二参数里(verbatim 课程 block 79 highlighted lines 26-30: `context: async ({ req }) => { const auth = req.headers.authorization; const currentUser = await getUserFromAuthHeader(auth); return { currentUser } }`)。**Apollo Server context 关键设计**:context 是 Apollo 给所有 resolvers 共享的"请求级对象",每个 HTTP 请求都重新调一次 context 函数,返回的 `{ currentUser }` 作为**第三个参数**传给所有 resolvers(per course block 83 "The context value is passed to resolvers as the third parameter")。**改** `.env.example` 加 `JWT_SECRET=replace-this-with-a-strong-random-secret` 占位。**db.js / index.js / models/person.js 全部沿用 part8t verbatim**(本节不涉及连接/启动/person 数据)。**`// filters missing` 占位仍保留** — User and log in 节**不**包含 phone filter。**故意不做**:不接 bcrypt / 不存 passwordHash / 不实现 addFriend / 不补 phone filter / 不接前端(课程本节是纯后端,前端 form 留后续小节)/ 不抽 env 验证 / 不重试 token 解析失败(per course verbatim jwt.verify 失败抛异常)/ 不处理 token 过期(per course 简化 jwt.sign 无 expiresIn)/ 不分离 User.input validation(SDL 的 `username: String!` 已够)。**运行前置**:同 part8t + JWT_SECRET 在 .env 里配好(随机字符串)+ Apollo Explorer 打开 GraphQL endpoint + (可选)postman 验证 HTTP header 格式。**验证**:Step 2 createUser 成功 + Step 3 createUser username 太短触发 GraphQLError + Step 4 login 成功拿 token + Step 5 login 失败 'wrong credentials' + Step 6 me query with token + Step 7 me query without token return null + Step 8 addPerson/editNumber 正常路径(part8t 回归)+ Step 9 me query with invalid token + Step 10 JWT_SECRET 缺失触发 login 抛。**下一步**是 part8v "Friends list"(per course 当前章节 block 87:User.friends 增删 + addAsFriend mutation + Person.friends 字段 — Chapter 4 第五小节)
- [`v-friends-list/`](./v-friends-list/) — Chapter 4 第 5 子节(addPerson 加鉴权 + 自动加 friends + 新增 addAsFriend)— **纯后端**(part8u 续):**改** `schema.js` 在 `Mutation` 块加 `addAsFriend(name: String!): User`(per course block 94 highlighted line 3)+ **改** `resolvers.js` 的 `Mutation.addPerson` 签名 `(root, args)` → `(root, args, context)` + 加 `currentUser = context.currentUser` + 加 `if (!currentUser) throw GraphQLError('not authenticated', { extensions: { code: 'UNAUTHENTICATED' }})`(per course block 91 highlighted lines 2-11)+ try 块加 `currentUser.friends = currentUser.friends.concat(person)` + `await currentUser.save()`(per course block 91 highlighted lines 28-29)+ **新增** `Mutation.addAsFriend: async (root, args, { currentUser }) => {...}` 完整 resolver(per course block 96:UNAUTHENTICATED 鉴权 + `nonFriendAlready = (person) => !currentUser.friends.map((f) => f._id.toString()).includes(person._id.toString())` helper + `Person.findOne({ name: args.name })` + 不存在 throw `GraphQLError("The name didn't found", { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name }})`(课程原文英文别扭但严格 verbatim)+ `if (nonFriendAlready(person)) currentUser.friends = currentUser.friends.concat(person)` + `await currentUser.save()` + return currentUser)+ `server.js` / `models/{user,person}.js` / `index.js` / `db.js` / `package.json` / `.env.example` 全部沿用 part8u verbatim + **课程故意**:editNumber **不**加鉴权(per course block 88 文字"adding and editing persons requires logging in"但 block 91-103 只展示 addPerson 不展示 editNumber,留后续章节)+ allPersons **不**按 `currentUser.friends` 过滤(per course block 88 文字提"personalized view"但 block 91-103 不展示,留后续)+ `// filters missing` 占位仍保留(phone filter 仍未补)+ **新课程错误码** `UNAUTHENTICATED`(per course block 91/96)+ **解构风格**:addAsFriend 用 ES6 解构 `(root, args, { currentUser })`(per course block 96 + 100),addPerson 用非解构 `(root, args, context)` + `const currentUser = context.currentUser`(per course block 91),课程故意两种都展示+ **addPerson 自动加到 friends**(block 91 highlighted lines 28-29 — `currentUser.friends.concat(person)` + `currentUser.save()` 内嵌在 person.save 的 try 块里,所以三者**事务一体**)+ **运行前置**:同 part8u + 需要带 `Authorization: Bearer <token>` 调 addPerson + addAsFriend(否则 UNAUTHENTICATED)+ (可选)mongosh 跑 block 89 一次性清理 `db.persons.deleteMany({ _id: { $nin: await db.users.distinct('friends') } })` 删掉不在任何 user.friends 里的 person+ **故意不做**:editNumber 鉴权 / allPersons friends filter / 修 "The name didn't found" 语法 / 抽 nonFriendAlready utility / 改 Person schema 加 friends 字段 / 做前端 / 改 server.js 或 models — Chapter 4 第五小节
- [`w-user-login-frontend/`](./w-user-login-frontend/) — **Chapter 5 第 1 子节**(前端范畴首次进入 — 独立 Vite + React + Apollo Client 项目)— 13 文件全新建(无 cp 自其他子目录):`.gitignore` + `.env.example` (VITE_BACKEND_URL 占位) + `package.json`(verbatim part8k 版本:React 19.2 + @apollo/client 3.11 + Vite 7.2.4)+ `vite.config.js`(verbatim part8k React plugin)+ `index.html`(Vite 入口)+ `src/main.jsx`(verbatim part8p ApolloProvider + **uri 改用 `import.meta.env.VITE_BACKEND_URL` 兜底 `'http://localhost:4000'`** — minimum viable addition 引入 env)+ `src/queries.js`(verbatim 课程 block 1 `LOGIN` mutation + **最小 stub ALL_PERSONS** query — 仅 `name` + `phone` 字段,address/id 等完整字段等 Chapter 5 "Listing persons"小节补)+ `src/components/Notify.jsx`(verbatim part8p 沿用 — 8 行错误展示)+ `src/components/LoginForm.jsx`(**verbatim 课程 block 3** 完整版 — useState × 2 + useMutation(LOGIN, { onCompleted, onError }) + **localStorage.setItem('phonebook-user-token', token) 持久化 token** + submit handler + form JSX)+ `src/components/Persons.jsx`(**STUB 占位** — per 用户选择"全部 stub 占位",只接收 persons prop 但渲染字面 "Persons here")+ `src/components/PersonForm.jsx`(**STUB 占位** — 渲染字面 "PersonForm here")+ `src/components/PhoneForm.jsx`(**STUB 占位** — 渲染字面 "PhoneForm here")+ `src/App.jsx`(**verbatim 课程 block 7 + 12 合并** — token 来自 localStorage + useApolloClient + onLogout + `client.resetStore()` 清 cache + `if (!token)` 走 LoginForm / 否则走 Persons/PersonForm/PhoneForm + logout 按钮)+ **9 个核心概念**:Apollo Client 标准前端工程 / queries.js 集中 / useMutation 双回调 / localStorage 持久化 / token 是登录状态唯一来源 / useApolloClient hook / cache resetStore 防泄露 / course block 拆分合并 / stub 占位策略+ **关键架构**:token 来自 localStorage 'phonebook-user-token' 持久化,刷新页面自动恢复登录;logout 同时清 React state + localStorage + Apollo cache 三处一致+ **运行前置**:同 part8v 后端跑着 + `npm install` + `cp .env.example .env` + `npm run dev`(双终端:后端 4000 + 前端 5173)+ **故意不做**:Persons/PersonForm/PhoneForm 完整版(等后续小节) / 自动加 Authorization header(per course 下一小节"Adding a token to a header")/ fixing validations(per course 第三小节)/ updating cache 精确控制(per course 第四小节)/ 修改密码 hashing(per part8u 简化 'secret')/ token 过期处理 — Chapter 5 第一小节

## Chapter 2 → Chapter 3 → Chapter 4 切换备忘(2026-08-26 → 2026-08-30)

> **重要状态切换**:Chapter 2(Apollo Server,part8a-j 共 10 个子项目)全部完结。下面进入 **Chapter 3: React and GraphQL** — 客户端侧 Apollo Client 接入。
>
> **Chapter 2 与 Chapter 3 的根本区别**:
> - Chapter 2:**服务端**(Node.js + Apollo Server v4 standalone)— 监听端口、暴露 SDL、跑 resolver
> - Chapter 3:**客户端**(浏览器 + React + Apollo Client)— 发 query/mutation、订阅响应、缓存、错误展示
>
> **Chapter 3 关键诚实声明**:
> - 网络/环境限制:本会话无法直接拉 MOOC.fi(超时)+ fullstackopen.com(已 redirect)+ GitHub 镜像(0 hit)— 子节切分需用户协助核对
> - 仍需遵循"verbatim 课程原文"纪律,每个子项目落地前必须从 MOOC.fi 拿到原文
>
> **Chapter 3 子项目命名约定**:沿用 part8a-j 的字母,继续 part8 **k** 起排(Apollo Client 接入通常以 "Apollo Client" 子节打头)
>
> **项目结构预想**:与 part8a-j 单文件 Node.js 不同,Chapter 3 起进入 Vite + React 标准前端工程
> ```
> part8/
> ├── k-apollo-client/      # part8k — 客户端 Apollo Client 接入
> │   ├── package.json      # React + Vite + @apollo/client + graphql
> │   ├── vite.config.js    # Vite 配置(可选)
> │   ├── index.html        # Vite 入口 HTML
> │   ├── src/
> │   │   ├── main.jsx      # React 入口 + ApolloProvider 包裹
> │   │   ├── App.jsx       # 顶层组件 + useQuery
> │   │   ├── queries.js    # gql 模板字符串(query/mutation 定义)
> │   │   └── components/   # 子组件
> │   └── README.md
> ```
>
> **服务依赖**:Chapter 3 子项目要发 query/mutation 到 **Chapter 2 的 server**(part8j 当前)。需要两个终端:① 一个跑 part8j server(端口 4000);② 一个跑 Chapter 3 Vite dev(默认端口 5173)。

## 后续子段

- **不**跳过 part7 c 章剩余子节(用户已确认跳过,从 part8 开始)
- **不** commit / push
- **不** 跑任何命令
- **一次只推进一小节** — 等用户确认 part8r 后再进 part8s
- part8o **Updating the cache 已完结**(refetchQueries + src/queries.js 抽出 + Apollo cache 三种策略对比)
- part8p **Handling mutation errors 已完结**(onError option + setError prop 链 + Notify 组件 + notify 函数 + setTimeout 10s 自动清除 + 兑现 part8h 抛 GraphQLError 的前端处理)
- part8q **Updating a phone number 已完结**(EDIT_NUMBER mutation + PhoneForm 组件 + **onCompleted 替代 onError 处理 success-but-null** + Apollo cache 自动更新兑现 + setError prop 链复用)
- part8r **Refactoring the backend 已完结**(单文件 Apollo Server → 4 文件模块化 + dotenv 读 PORT env + **factory function startServer(port) 模式** + 单一职责 SRP 拆分 + persons 数组从 schema.js 搬到 resolvers.js — Chapter 4 第一小节)
- part8s **Mongoose and Apollo 已完结**(in-memory persons 数组 → MongoDB 持久化 + **新增 db.js / models/person.js 两个模块** 共 6 文件 + mongoose ^8.9.0 + dotenv 读 MONGODB_URI + **main async 顶层启动模式** `await connectToDatabase` → `startServer` + resolvers 全部转 async 用 `Person.collection.countDocuments` / `Person.findOne` / `Person.exists` / `person.save` + **`// filters missing` 占位 comment 保留 verbatim** 不抢 part8t 内容 + `.env.example` 占位模板不创建真实 .env — Chapter 4 第二小节)
- part8t **Validation 已完结**(在 part8s 6 文件基础上**仅改 resolvers.js 一个文件** + addPerson + editNumber 两个 `await person.save()` 加 try/catch + catch 抛 `GraphQLError(`Saving person failed: ...` / `Saving number failed: ...`, { extensions: { code: 'BAD_USER_INPUT', invalidArgs: args.name, error }})` + **extensions 三字段设计** code/invalidArgs/error + **GraphQL ↔ Mongoose 双重防线** + try/catch 精准防御只包 save() + 前端 part8p `onError` 接住 error.message 显示红字 Notify 实现前后端错误链路闭环 + **`// filters missing` 占位仍保留** — 课程 Validation 段**不**包含 phone filter — Chapter 4 第三小节)
- part8u **User and log in 已完结**(在 part8t 6 文件基础上**新增 1 文件 + 改 5 文件** — 新建 `models/user.js` verbatim 课程 block 59 + 改 `package.json` 加 `jsonwebtoken: ^9.0.2` + 改 `schema.js` 加 `type User` + `type Token` + `Query.me: User` + `Mutation.createUser` + `Mutation.login` + 改 `resolvers.js` 顶部加 `const jwt` + `const User` + Query.me 三参数签名 `(root, args, context) => context.currentUser` + Mutation.createUser 用 `.save().catch(...)` 风格 + Mutation.login 用 `jwt.sign({ username, id }, JWT_SECRET)` 返回 `{ value: token }` + 改 `server.js` 顶部加 `const jwt` + `const User` + **新增** `getUserFromAuthHeader(auth)` helper + **新增** context 字段在 startStandaloneServer 里 + 改 `.env.example` 加 `JWT_SECRET` 占位 + 课程故意简化 password = 'secret' 硬编码、不用 bcrypt、不实现 addFriend(只预留 friends schema 字段)+ Apollo Server context 注入 `currentUser` 给所有 resolvers(per course block 83) + **`// filters missing` 占位仍保留** — 课程 User and log in 段**不**包含 phone filter — Chapter 4 第四小节)
- part8v **Friends list 已完结**(在 part8u 6 文件基础上**改 2 文件** — `schema.js` 加 `addAsFriend(name: String!): User` + `resolvers.js` 改 `Mutation.addPerson` 加 `(root, args, context)` 三参 + `currentUser = context.currentUser` + UNAUTHENTICATED 鉴权 + try 块加 `currentUser.friends.concat(person)` + `currentUser.save()` + **新增** `Mutation.addAsFriend` 完整 resolver 含 nonFriendAlready helper — Chapter 4 第五小节)
- part8w **User login 已完结**(Chapter 5 第 1 子节 — **part8 第一次进入前端范畴**,新目录 `w-user-login-frontend/` 完全独立 Vite + React + Apollo Client 工程 — 13 文件全新建无 cp 自其他子目录 — verbatim 课程 block 1 LOGIN mutation + verbatim 课程 block 3 LoginForm 完整组件含 useMutation 双回调 + localStorage 持久化 token + verbatim 课程 block 7+12 App.jsx 含 token 来自 localStorage + useApolloClient + client.resetStore logout — Persons/PersonForm/PhoneForm 三个 stub 占位组件 per 用户选择"全部 stub 占位" — `import.meta.env.VITE_BACKEND_URL` 兜底 `'http://localhost:4000'` minimum viable env 接入)
- 下一步是 **Chapter 5 第 2 小节 — Adding a token to a header**(课程下一小节 — per course H2,把 localStorage 的 token 自动加到 Apollo Client 的请求 header `Authorization: Bearer <token>`)
- Chapter 2 → Chapter 3 切换是**架构大切换**:Node.js 单文件 → Vite + React 多文件工程。需要用户从 part8k 起明确"哪个子节先做",且需要 part8j server 在跑才能验证
- Chapter 3 → Chapter 4 切换是**第二个架构大切换**:Vite + React 多文件 → Node.js 多文件模块化。这次没有 React 代码,纯后端 Apollo Server 重构(拆模块 + dotenv)。后续 Chapter 4 子节会接 MongoDB / mongoose / bcrypt / jsonwebtoken。part8r 是 Chapter 4 的"基础设施准备"子节,为后续 DB 接入留扩展空间。