# ⭐⭐⭐ .env.example — part8w 前端环境变量占位 ⭐⭐⭐
#
# ⭐ 关键诚实声明:本节是 part8 第一次进入前端范畴
#   - 前端用 Apollo Client 调后端 GraphQL endpoint
#   - 后端默认跑在 http://localhost:4000(part8u/v 沿用)
#   - Vite 通过 VITE_ 前缀的 import.meta.env 暴露变量给浏览器
#
# ⭐ 使用方法(per discipline "Claude 不替你跑命令"):
#   1. cp .env.example .env
#   2. 把 VITE_BACKEND_URL 改成实际后端地址(本地默认 http://localhost:4000)
#   3. src/main.jsx 用 import.meta.env.VITE_BACKEND_URL 读取
#
# ⭐ 注意:Vite 的 import.meta.env 只会暴露 VITE_ 开头的变量
#   这是 Vite 安全设计,避免泄露不带前缀的 secret

# ⭐⭐⭐ 后端 GraphQL endpoint 地址 ⭐⭐⭐
#
# ⭐ 默认值:http://localhost:4000 — part8u/v 后端启动后默认端口
#
# ⭐ 如果后端跑在别的机器(远程 Atlas + 部署的 server),改成对应 URL
#   例如 https://phonebook-api.example.com
VITE_BACKEND_URL=http://localhost:4000